[pytest]
python_files = *.py
python_functions = test_*
testpaths = tests
minversion = 3.0
addopts = --doctest-modules --cov={{cookiecutter.package_name}} --cov-fail-under 100
norecursedirs = management
